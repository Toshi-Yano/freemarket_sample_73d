- if item.errors.any?
  .error_explanation
    %ul.error_explanation__list
      - item.errors.full_messages.each do |message|
        %li 
          = icon('fas', 'exclamation-triangle')
          = message